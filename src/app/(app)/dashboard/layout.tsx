import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Command Hub — BizPocket',
}

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return children
}
